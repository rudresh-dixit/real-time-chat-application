// initiate our react application
import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App />, document.querySelector('#root'));
// target the root element inside of the html
// target the app component at the is root in our html file.
// app component is rendered at id root.